(loop [iteration 0]
  (println (str "Iteration " iteration))
  (if (> iteration 5)
    (println "End")
    (recur (inc iteration))))

(ns clojure
  (:gen-class))
(defn Example []
  (loop [x 10]
    (when (> x 1)
      (println x)
      (recur (- x 2)))))
(Example)

Dotimes

(ns clojure
  (:gen-class))
(defn kishore []
  (dotimes [n 5]
    (println "hai")))
(kishore)



