(ns clojure
  (:gen-class))
(defn recur []
  (loop [i 0]
    (when (< i 5)
      (println "*")
      (recur (inc i)))))
(recur)

(ns clojure
  (:gen-class))
(defn recur []
  (loop [i 0]
    (when (< i 5)
      (println i)
      (recur (dec i)))))
(recur)