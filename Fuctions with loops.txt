(ns clojure.examples.hello
  (:gen-class))
(defn add-2
  "add this numbers"
  [a b]
  (let [c (+ a b)]
    c))

(defn factorial [n]
  (if (> n 1)
    (* n (factorial (dec n)))
    1))
(factorial 8)
(dec 8)
(inc 8)